{
    "name": "@ermish/parcel-transformer-shuji-tests",
    "version": "1.0.0",
    "scripts": {
        "test": "yarn --check-files && parcel build ./markdown/** --no-cache --log-level verbose",
        "testold": "jest --verbose"
    },
    "dependencies": {
        "@ermish/parcel-transformer-shuji": "file:../",
        "@ermish/shuji": "1.1.3"
    },
    "devDependencies": {
        "jest": "^26.6.3",
        "parcel": "2.0.0-beta.2"
    }
}
